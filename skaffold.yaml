apiVersion: skaffold/v1
kind: Config
metadata:
  name: cvision-
build:
  artifacts:
  - image: opencv
    sync:
      manual:
      - src: 'src/**/*.py'
        dest: .
        strip: src/
      - src: 'src/static/**'
        dest: .
        strip: src/
    # kaniko:
      # image: gcr.io/kaniko-project/executor:debug
    #   image: agilestacks/sleepy 
    #   # buildArgs:{}
profiles:
- name: nucs1
  activation:
  - kubeContext: nucs1.kubeflex.dev.superhub.io
- name: anton1
  activation:
  - kubeContext: anton1.antondemo0101.dev.superhub.io
- name: incluster
  build:
    tagPolicy:
      dateTime: {}
    cluster:
      dockerConfig: 
        secretName: dockerconfig
deploy:
  kubectl:
    manifests:
      - k8s/*
